# Central Bus v0.6 — Dependencies
# PRD Section 9: All versions pinned for reproducibility

fastapi==0.115.0
uvicorn[standard]>=0.31.1
aiosqlite==0.20.0
pydantic>=2.11.0
starlette>=0.37.2,<0.39.0       # Pinned: v1.x breaks fastapi 0.115.0 APIRouter (on_startup removed)
pydantic_settings>=2.0.0        # For central_bus.config.BaseSettings

# Test only
httpx>=0.27.1
