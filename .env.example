# SoloCorp OS — Environment Variables
# copy ไปเป็น .env:  cp .env.example .env
# (สำหรับ private repo — commit .env ได้เลย)

SOLOCORP_API_KEY=sk-solocorp-admin-local-dev-001
SOLOCORP_BUS_URL=http://127.0.0.1:8099
